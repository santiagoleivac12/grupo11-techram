<!DOCTYPE html>
<html lang="es">

<%- include('../partials/head') %>

    <body>
        <%- include('../partials/header') %>
        <main>
            <div class="containerAvatar">
                <img id="imagenPrevisualizacion" src="/images/avatars/<%= user.avatar %>" alt="" class="w-full cover">
                <input type="file" name="avatar" id="seleccionArchivos" value="<%= user.avatar %>" accept="image/png, image/jpg, image/jpeg, image/gif">
              </div>
            <!-- /*<p class="containerAvatar"><img class="avatarProfile" src="/images/avatars/<%user.id %>?_method=PUT" method="post" enctype="multipart/form-data" alt="avatar"></p> -->
            <p class="bienvenido">Tu perfil <b> <% if (typeof session.user != "undefined") { %>
                <span> <%= session.user.name %> </span>  
                <div class="datosPersonales">
                    <p class="titleProfile">Datos Personales</p>
                  <p class="firstNameProfile"> <h4 class="nameProfile">Nombre:</h4><%= user.firstName %> </p>
                  <p class="lastNameProfile"><h4 class="nameProfile">Apellido:</h4> <%= user.lastname %> </p>
                  <p class="emailProfile"><h4 class="nameProfile">Email:</h4> <%= user.email %></p> 
                  <!-- <p class="phoneProfile"><h4 class="nameProfile">Teléfono:</h4> <%= user.phone %></p> -->
                  </div>
              <% } else {%>
                Usuario
              <% } %> 
           </div>
           <div class="datosPersonales" >
               <p class='titleProfile'>Datos de Envio</p>
            <p class="streetProfile"><h4 class="nameProfile">Dirección:</h4> <%= user.addresses.street %></p>
            <p class="numberProfile"><h4 class="nameProfile">Número:</h4> <%=session.user.address %></p>
            <p class="cityProfile"><h4 class="nameProfile">Ciudad:</h4> <%= session.user.address %></p>
            <p class="provinceProfile"><h4 class="nameProfile">Provincia:</h4> <%= session.user.address %></p>
            <p class="postalCodeProfile"><h4 class="nameProfile">Código Postal:</h4> <%= session.user.address %></p>
            
           </div>
           <button class="buttonProfile">Editar Información</button>
        </main>





            <%- include('../partials/footer') %>
    </body>

</html>